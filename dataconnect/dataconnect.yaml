specVersion: "v1"
serviceId: "rainscare"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "rainscare-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./example"]
